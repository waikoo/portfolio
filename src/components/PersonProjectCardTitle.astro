---
import "@fontsource/jost/700.css";

const { color, className, isInverted  } = Astro.props;
const justifySelf = isInverted
  ? "lg:justify-self-start lg:text-left"
  : "lg:justify-self-end lg:text-right";
---

<p
  style={{ color: color }}
  class={`font-['Jost'] font-bold text-[28px] sm:text-[32px] 2xl:text-[38px] tracking-[-0.1em] barna-project text-center leading-none ${justifySelf} ${className}`}
>
  <slot />
</p>
