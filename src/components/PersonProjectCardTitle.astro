---
import "@fontsource/jost/700.css";

const { color, className, isInverted } = Astro.props;
const justifySelf = isInverted
  ? "lg:justify-self-start"
  : "lg:justify-self-end";
---

<p
  style={{ color: color }}
  class={`sm:text-[32px] tracking-[-0.1em] barna-project ${justifySelf} ${className}`}
>
  <slot />
</p>

<style>
  p {
    font-family: "Jost", sans-serif;
    font-weight: 700;
    line-height: 0.98;
  }
</style>
