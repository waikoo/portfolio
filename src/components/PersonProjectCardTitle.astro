---
import "@fontsource/jost/700.css";

const { color, className } = Astro.props;
---

<p
  style={{ color: color }}
  class={`sm:text-[32px] tracking-[-0.1em] -mb-[15px] barna-project ${className}`}
>
  <slot />
</p>

<style>
  p {
    font-family: "Jost", sans-serif;
    font-weight: 700;
    line-height: 0.98;
  }
</style>
