---
const { name, surname, occupation, color, gridColumn, gridRow } = Astro.props;
---

<div
  style={`
  color: ${color};
  grid-column: ${gridColumn};
  grid-row: ${gridRow};`}
  class=`text-[190px] gap-0 flex flex-col ${surname.toLowerCase()}-name justify-content-start ml-10 mt-6`
>
  <p class="-mb-[9.5rem]">{name}</p>
  <p class="m-0">{surname}</p>
  <!-- <span class="text-[3rem] mt-[-0.5rem]">{occupation}</span> -->
</div>

<style>
  @font-face {
    font-family: "Jost";
    src: url("/fonts/Jost-ExtraLight.ttf") format("truetype");
    font-weight: 200;
    font-style: normal;
    font-display: swap;
  }

  div {
    font-family: "Jost";
  }
</style>
