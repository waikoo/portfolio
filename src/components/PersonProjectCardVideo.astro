---
const { src, id } = Astro.props;
---

<video
  class="rounded-[20px]"
  autoplay
  loop
  muted
  playsinline
  transition:persist
  width={id === 4 ? 200 : 640}
>
  <source type="video/mp4; codecs=av01" src={src[0]} />
  <source type="video/mp4" src={src[1]} />
</video>
