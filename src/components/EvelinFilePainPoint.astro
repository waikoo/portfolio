---
import IconTriangleMobile from "./IconTriangleMobile.astro";

const { bgColor, className } = Astro.props;
---

<p class={`relative text-[13px] xl:text-[18px] flex items-center ${className}`}>
  <IconTriangleMobile bgColor={bgColor} className="mr-2" />
  <slot />
</p>

<style>
  p {
    font-family: "Jost", sans-serif;
  }
</style>
