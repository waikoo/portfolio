---
import "../styles/global.css";
import HamburgerMenu from "../components/HamburgerMenu.astro";
import Analytics from "@vercel/analytics/astro";
import SpeedInsights from "@vercel/speed-insights/astro";

interface Props {
  title: string;
  bgColor?: string;
  fullName?: string;
}

const { fullName, title, bgColor } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta property="og:title" content="Team Szász" />
    <meta
      name="description"
      content="The joint portfolio of Szász Evelin (UI/UX Designer) and Szász Barna (Front-End Developer)"
    />
    <meta property="og:image" content="/images/og_image_common.avif" />
    <meta property="og:url" content="https://szasz.dev" />
    <meta property="og:type" content="website" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <SpeedInsights />
    <Analytics />
    <title>{title}</title>
  </head>
  <body style={{ backgroundColor: bgColor }}>
    <slot />
    {!fullName ? null : <HamburgerMenu fullName={fullName} bgColor={bgColor} />}
  </body>
</html>

<style is:global>
  *,
  *:before,
  *:after {
    box-sizing: border-box;
  }

  html {
    scroll-padding-top: 140px;
  }
</style>
