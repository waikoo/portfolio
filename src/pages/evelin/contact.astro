---
import ContactForm from "../../components/ContactForm.astro";
import PersonContactItem from "../../components/PersonContactItem.astro";
import PersonLayout from "../../components/PersonLayout.astro";
import Layout from "../../layouts/Layout.astro";
import { extractName } from "../../utils/extractName";

const fullName = "Szász Evelin";
const { name } = extractName(fullName);
---

<Layout title={`Contact ${fullName}`} fullName={fullName}>
  <PersonLayout fullName={fullName}>
    <main
      class="relative flex justify-center items-center content-center my-[120px]"
    >
      <div
        class="flex flex-col gap-[10px] justify-center items-center text-center"
      >
        <PersonContactItem
          name={name}
          title="Email"
          value="neolor@protonmail.com"
        />
        <PersonContactItem
          name={name}
          title="Dribbble"
          value="https://dribbble.com/Lyonixa"
        />
        <PersonContactItem
          name={name}
          title="LinkedIn"
          value="https://www.linkedin.com/in/evelin-szász-3ba323358/"
        />
      </div>
    </main>
    <div
      class="animate-fade-right animate-once animate-duration-[1500ms] animate-ease-in-out"
    >
      <ContactForm lowercaseName="evelin" />
    </div>
  </PersonLayout>
</Layout>
